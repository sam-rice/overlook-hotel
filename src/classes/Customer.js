class Customer {
  constructor(object) {
    this.id = object.id
    this.name = object.name
    // this.allBookings = this.getAllBookings()
  };

  // getAllBookings() {

  // };

};

module.exports = Customer;